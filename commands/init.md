---
description: Initialize Harness Engineer state and project configuration.
allowed-tools: Bash
argument-hint: [--profile node|generic]
---

Run `harness init $ARGUMENTS`. Report the created `.harness-engineer/` state files and `harness/` configuration directories.
