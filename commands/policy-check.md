---
description: Check a shell command against the Harness Engineer policy.
allowed-tools: Bash
argument-hint: --command "<cmd>"
---

Run `harness policy-check $ARGUMENTS` and report whether the command is allowed, warns, requires approval, or is blocked.
